[start_tftpd]
command:/usr/local/etc/rc.d/netboot_tftpd start
parameters:
type:script
message:starting netboot tftpd

[stop_tftpd]
command:/usr/local/etc/rc.d/netboot_tftpd stop
parameters:
type:script
message:stopping netboot tftpd

[restart_tftpd]
command:/usr/local/etc/rc.d/netboot_tftpd restart
parameters:
type:script
message:restarting netboot tftpd

[status_tftpd]
command:/usr/local/etc/rc.d/netboot_tftpd status; exit 0
parameters:
type:script_output
message:request netboot tftpd status

[start_http]
command:/usr/local/etc/rc.d/netboot_http start
parameters:
type:script
message:starting netboot http

[stop_http]
command:/usr/local/etc/rc.d/netboot_http stop
parameters:
type:script
message:stopping netboot http

[restart_http]
command:/usr/local/etc/rc.d/netboot_http restart
parameters:
type:script
message:restarting netboot http

[status_http]
command:/usr/local/etc/rc.d/netboot_http status; exit 0
parameters:
type:script_output
message:request netboot http status

[start_sftp]
command:/usr/local/etc/rc.d/netboot_sftp start
parameters:
type:script
message:starting netboot sftp

[stop_sftp]
command:/usr/local/etc/rc.d/netboot_sftp stop
parameters:
type:script
message:stopping netboot sftp

[restart_sftp]
command:/usr/local/etc/rc.d/netboot_sftp restart
parameters:
type:script
message:restarting netboot sftp

[status_sftp]
command:/usr/local/etc/rc.d/netboot_sftp status; exit 0
parameters:
type:script_output
message:request netboot sftp status

[reload]
command:configctl template reload OPNsense/Netboot
parameters:
type:script
message:reloading netboot configuration

[setup]
command:/usr/local/opnsense/scripts/netboot/setup.sh
parameters:
type:script_output
message:ensuring netboot runtime user and directories

# IMPORTANT: %s belongs ONLY in `parameters:`, NEVER in `command:`. configd's
# base.py::_cmd_builder substitutes the user-supplied arguments into the
# `parameters:` template and then returns `command + " " + parameters`. If
# command also has %s, that %s is passed verbatim as an extra argv element
# to the script -- which then mis-parses its real positional args. Symptom:
# script falls through to the unknown-preset branch, prints an error to
# stderr, exits non-zero, configd's script_output captures empty stdout
# and returns 0 bytes. Hours of "Bootstrap does nothing" went into finding
# this. See base.py:_cmd_builder upstream.

[fetch_url]
command:/usr/local/opnsense/scripts/netboot/fetch_url.sh
parameters:%s %s
type:script_output
message:fetching url to netboot content root

# Generic 'bootstrap <preset>' entry point. configd substitutes the first
# whitespace-separated parameter into %s, so the caller invokes this as
# `configctl netboot bootstrap netboot_xyz` / `configctl netboot bootstrap ipxe`
# / etc. The script validates the preset name and refuses unknown values,
# so this is safe to expose.
[bootstrap]
command:/usr/local/opnsense/scripts/netboot/bootstrap.sh
parameters:%s
type:script_output
message:fetching iPXE bootstrap binaries (BIOS + UEFI) for the selected preset

# Backwards-compatible alias for the original single-preset action. Older
# clients (or anything that hand-types `configctl netboot bootstrap_netboot_xyz`)
# still get the netboot.xyz binaries. New code should use [bootstrap] above.
[bootstrap_netboot_xyz]
command:/usr/local/opnsense/scripts/netboot/bootstrap.sh netboot_xyz
parameters:
type:script_output
message:fetching netboot.xyz iPXE bootstrap (BIOS + UEFI) -- alias for [bootstrap] netboot_xyz
